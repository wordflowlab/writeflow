/** Thinking 提示：约束与输出规范（最小文档） */
export const THINKING_GUIDE = `
## Thinking Output
- 在需要展示推理过程时，使用 <thinking>…</thinking> 包裹
- 正文中不要保留 thinking 片段，避免干扰输出

## Tooling
- 如需从已有文本中提取思维链，调用 thinking_extract 工具，并传入 text 字段。
`

