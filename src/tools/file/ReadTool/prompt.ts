const MAX_LINES_TO_READ = 2000
const MAX_LINE_LENGTH = 2000

export const DESCRIPTION = '从本地文件系统读取文件内容'

export const PROMPT = `从本地文件系统读取文件内容。

基本要求:
- file_path 参数必须是绝对路径，不能是相对路径
- 默认从文件开头读取最多 ${MAX_LINES_TO_READ} 行
- 可以选择指定行偏移量和限制行数（特别适用于大文件）
- 超过 ${MAX_LINE_LENGTH} 字符的行会被截断
- 结果使用 cat -n 格式返回，行号从 1 开始

支持的文件类型:
- 文本文件：直接读取内容
- 图像文件：显示图像内容（PNG、JPG、GIF 等）
- PDF 文件：逐页提取文本和视觉内容
- Jupyter 笔记本：返回所有单元格及其输出

使用指南:
1. 对于小文件（< 2000 行），直接读取整个文件，不提供 offset 和 limit 参数
2. 对于大文件，先读取开头部分了解结构，然后使用 offset 和 limit 读取特定部分
3. 读取前可以使用 Bash 工具的 ls 命令确认文件存在
4. 如果需要搜索文件内容，建议配合 Grep 工具使用

安全注意事项:
- 只能读取文件，不能读取目录
- 工具会自动处理文件编码和换行符
- 空文件会返回系统提醒警告
- 支持读取临时文件和截图文件

常见错误处理:
- 文件不存在：返回明确的错误信息
- 权限不足：提示权限问题
- 文件过大：使用 offset/limit 分批读取
- 二进制文件：自动识别并适当处理

最佳实践:
- 读取配置文件时，先了解其格式和结构
- 读取代码文件时，关注导入语句和关键函数
- 读取日志文件时，从末尾开始读取最新内容
- 读取文档文件时，注意文件的整体结构和章节划分`

export const TOOL_NAME_FOR_PROMPT = 'Read'